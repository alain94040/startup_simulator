# Open Questions

## Characters / Unlock Conditions

- **Ryan unlock too early**: Ryan (angel) currently unlocks at `week >= 8 && advisors >= 1`, with no product or market fit requirement. He can appear when the player has barely built anything or validated demand. Should he require a product/signal floor (e.g. `product >= 20 && signal >= 15`) or a traction signal (e.g. `waitlist >= 5 || customers >= 1`)? Depends on whether Ryan is a "warm contact to keep warm over time" or a "traction-gated investor."
